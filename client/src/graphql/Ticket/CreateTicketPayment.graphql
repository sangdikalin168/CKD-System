mutation CreateTicketPayment(
  $ticketCode: String!
  $price: Float!
  $userId: Float!
) {
  CreateTicketPayment(
    ticket_code: $ticketCode
    price: $price
    user_id: $userId
  ) {
    payment_id
    code
    success
    message
  }
}
